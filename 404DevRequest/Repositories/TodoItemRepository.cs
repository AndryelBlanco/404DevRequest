using _404DevRequest.Models;
using _404DevRequest.Repositories.Interfaces;
using MongoDB.Driver;

namespace _404DevRequest.Repositories
{
    public class TodoItemRepository 
    {
        //Instancia do DbCOntext
        //MongoContext dbContext = new MongoContext();

        //public async void CreateTodoItem(TodoItem newTodo)
        //{
        //    await dbContext.TodoItem.InsertOneAsync(newTodo);
        //}
    }
}
